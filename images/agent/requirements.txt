# Coyote Agent Dependencies
# Pinned for reproducibility - update periodically

# Core
streamlit==1.41.1
python-dotenv==1.0.1
sentence-transformers==3.3.1
# torch is deliberately NOT pinned here. bot.Dockerfile installs the
# CPU-only wheel from PyTorch's CPU index in an earlier, separate step
# (same convention as images/core/core_analysis/requirements.txt).
# Re-adding it here resolves it from the default PyPI index, which on
# amd64 is the CUDA build: ~2.7 GB of unusable nvidia-*-cu12 packages.

# LangChain ecosystem (1.x series)
langchain-core==1.2.16
langchain-neo4j==0.7.0

# LLM provider integrations
langchain-openai==1.1.10
langchain-ollama==1.0.1
langchain-aws==1.3.1
langchain-huggingface==1.2.0
langchain-google-genai==4.2.1

# Neo4j driver
neo4j==5.26.0

# Testing
pytest==8.3.4
